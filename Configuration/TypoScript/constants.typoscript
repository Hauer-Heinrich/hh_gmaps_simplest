plugin {
    tx_hhgmapssimplest {
        view {
            # cat=hh_gmaps_simplest/file; type=string; label=Path to template root (FE)
            templateRootPath =

            # cat=hh_gmaps_simplest/file; type=string; label=Path to template partials (FE)
            partialRootPath =

            # cat=hh_gmaps_simplest/file; type=string; label=Path to template layouts (FE)
            layoutRootPath =
        }
    }
}
